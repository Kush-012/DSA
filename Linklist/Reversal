class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode rev=null;
        while(head!=null){
            ListNode nextref=head.next;
            head.next=rev;
            rev=head;
            head=nextref;
        }
        return rev;
    }
} 
